{
    "require": {
        "php": ">=8.0"
    },
    "require-dev": {
        "glpi-project/tools": "^0.7"
    },
    "config": {
        "optimize-autoloader": true,
        "platform": {
            "php": "8.0.0"
        },
        "sort-packages": true
    }
}
